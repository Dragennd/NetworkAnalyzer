<UserControl x:Class="NetworkAnalyzer.Utilities.ManagePresets"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:local="clr-namespace:NetworkAnalyzer.Utilities"
             mc:Ignorable="d"
             Height="278"
             Width="400">

    <Grid>
        <Border Panel.ZIndex="1" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button
                VerticalAlignment="Top"
                HorizontalAlignment="Right"
                Command="{Binding CloseWindowCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Style="{StaticResource GeneralButtonTemplate}">
                <materialIcons:MaterialIcon Kind="Close" Width="25"/>
            </Button>
        </Border>

        <Border
            CornerRadius="4"
            BorderThickness="1"
            BorderBrush="{DynamicResource BorderBrush}"
            Background="{DynamicResource MainWindowBackground}">

            <Border.Effect>
                <DropShadowEffect/>
            </Border.Effect>

            <StackPanel Orientation="Vertical">
                <TextBox Text="Presets" Margin="0" Style="{StaticResource GeneralHeader}"/>

                <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">
                    <ComboBox Width="260" Height="30" ItemsSource="{Binding PresetNames, RelativeSource={RelativeSource AncestorType=UserControl}}" DisplayMemberPath="PresetName"/>
                    <Button Command="{Binding DeletePresetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource GeneralButtonTemplate}">
                        <materialIcons:MaterialIcon Kind="TrashCanOutline" Width="30"/>
                    </Button>
                    <Button Command="{Binding SavePresetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource GeneralButtonTemplate}">
                        <materialIcons:MaterialIcon Kind="ContentSave" Width="30"/>
                    </Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0,0,10,0">
                        <Label Content="Preset Name" Style="{StaticResource OverviewCardHeader}"/>
                        <TextBox Width="175" Text="{Binding PresetName, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource RoundedTextBox}"/>

                        <Label Content="Target Address" Style="{StaticResource OverviewCardHeader}"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBox Width="120" Text="{Binding TargetName, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource RoundedTextBox}"/>
                            <Button FontSize="16" Command="{Binding AddItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource GeneralButtonTemplate}">
                                <materialIcons:MaterialIcon Kind="PlusBoxOutline" Width="25"/>
                            </Button>
                            <Button FontSize="16" Command="{Binding RemoveItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource GeneralButtonTemplate}">
                                <materialIcons:MaterialIcon Kind="MinusBoxOutline" Width="25"/>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                    <ListBox Margin="10,0,0,0" Height="170" Width="175" d:ItemsSource="{d:SampleData ItemCount=5}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
